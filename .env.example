# This file is intentionally non-secret. App clients use app-local .env files
# based on their own .env.example. Server secrets stay in Firebase or CI secrets.
SPACEMAN_ENVIRONMENT=development
FIREBASE_PROJECT_ID=replace-with-shared-dev-project-id
